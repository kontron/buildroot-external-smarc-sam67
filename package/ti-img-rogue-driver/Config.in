config BR2_PACKAGE_TI_IMG_ROGUE_DRIVER
        bool "ti-img-rogue-driver"
        depends on BR2_LINUX_KERNEL
	select BR2_LINUX_NEEDS_MODULES # not using kernel-module infra
        help
          Build the pvrsrvkm.ko kernel module.
